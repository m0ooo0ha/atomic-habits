# 🚀 دليل البدء السريع

## ✅ تم الإعداد بنجاح!

لقد تم إعداد مفتاح Claude API بنجاح. يمكنك الآن البدء فوراً!

---

## 🎯 التشغيل الآن

```bash
# شغّل المشروع
npm run dev
```

**افتح المتصفح على:** http://localhost:3000

---

## 📱 كيفية الاستخدام

### 1. إضافة متابعة لاعب

1. اختر **"لاعب"**
2. اكتب اسم لاعب مشهور، مثل:
   - محمد صلاح
   - كريستيانو رونالدو
   - ليونيل ميسي
   - نيمار
3. اضغط **"إضافة متابعة"**

### 2. إضافة متابعة نادي

1. اختر **"نادي"**
2. اكتب اسم النادي، مثل:
   - ليفربول
   - ريال مدريد
   - برشلونة
   - مانشستر يونايتد
3. اضغط **"إضافة متابعة"**

### 3. تفعيل الإشعارات

- اضغط على زر **"تفعيل الإشعارات"** في أعلى الصفحة
- اسمح للمتصفح بإرسال الإشعارات
- ستحصل على إشعار فوري عند حدوث انتقال!

---

## 🔍 اختبار الفحص اليدوي

لاختبار نظام الفحص بدون انتظار 30 دقيقة:

```bash
# في terminal جديد
curl -X POST http://localhost:3000/api/check-transfers \
  -H "Authorization: Bearer transfer_alert_secret_2026_yAAZX"
```

سترى استجابة JSON تحتوي على نتائج الفحص لجميع المتابعات.

---

## 🌐 النشر على الإنترنت

### الطريقة 1: Vercel (موصى به - مجاني)

1. اذهب إلى [vercel.com](https://vercel.com)
2. سجل دخول بحساب GitHub
3. انقر **"New Project"**
4. اختر repository: `m0ooo0ha/atomic-habits`
5. اختر branch: `claude/player-transfer-alerts-yAAZX`
6. أضف المتغيرات البيئية:
   ```
   ANTHROPIC_API_KEY=sk-7e7d879b70324c4995fcf584ff380d81
   CRON_SECRET=transfer_alert_secret_2026_yAAZX
   ```
7. انقر **"Deploy"**

**✅ المميزات:**
- رابط مباشر: `your-app.vercel.app`
- Cron Job يعمل تلقائياً كل 30 دقيقة
- SSL/HTTPS مجاني
- Serverless بدون تكاليف خادم

### الطريقة 2: Netlify

1. اذهب إلى [netlify.com](https://netlify.com)
2. استورد من GitHub
3. أضف المتغيرات البيئية
4. للـ Cron: استخدم Netlify Functions + Scheduled Functions

---

## 💰 تكلفة الاستخدام

### Claude API
- **الفحص الواحد**: ~$0.0001-0.0002 (أقل من 1/10 سنت)
- **10 متابعات × كل 30 دقيقة**:
  - 480 فحص يومياً
  - التكلفة: ~$0.05-0.10 يومياً (~$3 شهرياً)

### Vercel
- **Plan المجاني يكفي تماماً**:
  - 100 GB Bandwidth
  - Serverless Functions مجاني
  - Cron Jobs مجاني

**💡 نصيحة**: تابع فقط 3-5 لاعبين/أندية مهمين لتقليل التكلفة.

---

## 🎨 تخصيص مدة الفحص

لتغيير مدة الفحص التلقائي، افتح `vercel.json`:

```json
{
  "crons": [
    {
      "path": "/api/check-transfers",
      "schedule": "*/30 * * * *"    ← غيّر هنا
    }
  ]
}
```

**أمثلة:**
- `*/15 * * * *` = كل 15 دقيقة
- `*/60 * * * *` = كل ساعة (أوفر)
- `0 */2 * * *` = كل ساعتين
- `0 9,18 * * *` = مرتين يومياً (9 صباحاً و 6 مساءً)

---

## 🔧 استكشاف الأخطاء

### المشكلة: "Failed to fetch subscriptions"
**الحل:**
```bash
# تأكد من وجود مجلد data
mkdir -p data
```

### المشكلة: "API request failed"
**الحل:**
1. تأكد من صحة API Key
2. تحقق من رصيد حساب Anthropic
3. تأكد من الاتصال بالإنترنت

### المشكلة: الإشعارات لا تعمل
**الحل:**
1. فعّل الإشعارات في إعدادات المتصفح
2. استخدم HTTPS (Vercel أو localhost)
3. جرب متصفح آخر (Chrome/Firefox)

---

## 📊 مراقبة الاستخدام

### عرض Logs المحلية
```bash
npm run dev
# راقب Terminal للأخطاء
```

### عرض Logs على Vercel
1. اذهب إلى Vercel Dashboard
2. اختر المشروع
3. اذهب إلى **"Logs"** أو **"Functions"**
4. شاهد نتائج كل فحص تلقائي

---

## 🎯 نصائح للاستخدام الأمثل

### ✅ افعل:
- ✅ تابع لاعبين قريبين من الانتقال
- ✅ تابع الأندية في فترة الميركاتو
- ✅ حذف المتابعات غير المهمة
- ✅ فعّل الإشعارات للحصول على تنبيهات فورية

### ❌ لا تفعل:
- ❌ إضافة عشرات المتابعات (تكلفة عالية)
- ❌ مشاركة API Key مع الآخرين
- ❌ نشر `.env.local` على GitHub
- ❌ تقليل مدة الفحص لأقل من 15 دقيقة (استهلاك كبير)

---

## 🆘 الدعم

إذا واجهت أي مشكلة:
1. راجع ملف `SETUP_GUIDE.md` للتفاصيل الكاملة
2. راجع `README.md` للوثائق الشاملة
3. تحقق من Console المتصفح (F12)
4. راجع Terminal logs

---

## 🎉 جاهز للاستخدام!

```bash
# ابدأ الآن
npm run dev

# افتح
# http://localhost:3000
```

**استمتع بتتبع انتقالات لاعبيك المفضلين!** ⚽🔥
